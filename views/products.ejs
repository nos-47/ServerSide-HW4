<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Product List</title>
</head>
<body>
  <h1>All Products</h1>
  <% if (products.length > 0) { %>
    <ul>
      <% products.forEach(p => { %>
        <li>
          <strong><%= p.name %></strong> - $<%= p.price %>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No products found.</p>
  <% } %>
</body>
</html>
